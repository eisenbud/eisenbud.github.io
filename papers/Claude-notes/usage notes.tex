The --start flag lets you set the starting index number for any new batch
After a first-pass run, check the output for lines flagged as "year found in journal field" and add corrections to the MANUAL_FIXES dict at the top of the script
The BOOK_KEYWORDS list at the top is easy to extend if new series names appear
The awk fix note is documented in the header comment so you won't forget it